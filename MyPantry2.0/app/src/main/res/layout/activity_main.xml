<?xml version="1.0" encoding="utf-8"?>

<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.example.z.mypantry20.MainActivity"
    android:background="@android:color/black"
    >

    <ImageView
        android:layout_height="wrap_content"
        app:srcCompat="@drawable/home"
        android:id="@+id/Background"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:layout_width="match_parent"
        android:scaleType="fitXY" />

    <Button
        android:text="Get Started"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="67dp"
        android:id="@+id/getStartedButton"
        android:layout_alignBottom="@+id/continueButton"
        android:layout_centerHorizontal="true" />

    <Button
        android:text="Continue"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/continueButton"
        android:layout_marginBottom="18dp"
        android:layout_alignParentBottom="true"
        android:layout_alignLeft="@+id/getStartedButton"
        android:layout_alignStart="@+id/getStartedButton"
        android:layout_alignRight="@+id/getStartedButton"
        android:layout_alignEnd="@+id/getStartedButton" />

</RelativeLayout>